{
  "name": "spectra",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/server",
    "packages/web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev -w @spectra/server",
    "dev:web": "npm run dev -w @spectra/web",
    "dev:sandbox": "docker compose up -d --build && npm run dev:web",
    "sandbox:logs": "docker compose logs -f",
    "sandbox:down": "docker compose down",
    "test": "npm run test -w @spectra/core && npm run test -w @spectra/server && npm run test -w @spectra/web",
    "typecheck": "tsc -p packages/core && tsc -p packages/server && tsc -p packages/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
