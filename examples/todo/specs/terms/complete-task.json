{
  "name": "completeTask",
  "type": "function",
  "spec": "Marks a Task as done. If the Task is already done, this is a no-op (idempotent) — it does not error and does not un-complete it. For a RecurringTask, completing the current occurrence advances dueDate to the next occurrence computed from recurrenceRule and leaves the Task not done, so it is immediately open again for that next occurrence.",
  "parent": null,
  "tags": [],
  "attributes": [
    { "name": "target", "valueType": "ref:Task" }
  ]
}
